<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.health.medicare.emedicare">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectMedicineCategoryActivity" />
        <activity android:name=".SelectMedicineActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DoctorListActivity" />
        <activity android:name=".RegisterNowActivity" />
        <activity android:name=".PlaceOrderActivity" />
        <activity android:name=".SelectDoctorCategoryActivity" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".IntroductionActivity"></activity>
    </application>

</manifest>